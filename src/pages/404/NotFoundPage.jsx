import './NotFoundPage.scss';

function NotFoundPage() {
    return (
        <div className='not-page'>
            <h2 className='title'>Помилка 404 <br/> Такої сторінки не існує</h2>
        </div>
    );
}

export default NotFoundPage;
